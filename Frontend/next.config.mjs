/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ['api.jrrecyclingsolutionsltd.com.bd'],
    },
    // React Strict Mode
    reactStrictMode: true,
    // Additional custom Next.js configuration can be added here
  };
  
  export default nextConfig;
  
